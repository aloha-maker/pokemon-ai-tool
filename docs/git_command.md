# チェックアウト
グラフを右クリックして、ブランチを作成



# マージ

## 1.マージ先のブランチをチェックアウト
```bash
 $ git checkout dev 
 ```

## 2.マージ元のブランチを指定してマージ
```bash
 $ git merge パーティ欄UIの変更
 ```
## 3.gitのUIにマージ履歴が出るのでコミット＆プッシュ（変更の同期）
